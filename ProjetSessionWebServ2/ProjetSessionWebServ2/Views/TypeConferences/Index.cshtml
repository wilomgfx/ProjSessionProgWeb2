@model IEnumerable<ProjetSessionWebServ2.Models.TypeConference>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink(@ProjetSessionWebServ2.GlobalRessources.TypeRes.Create, "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>
        <td>
            @Html.ActionLink(@ProjetSessionWebServ2.GlobalRessources.TypeRes.Edit, "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink(@ProjetSessionWebServ2.GlobalRessources.TypeRes.Delete, "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
